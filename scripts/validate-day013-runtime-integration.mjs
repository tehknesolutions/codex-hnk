import fs from 'node:fs';
import path from 'node:path';
const root=process.cwd();const read=p=>fs.readFileSync(path.join(root,p),'utf8');const json=p=>JSON.parse(read(p));const fail=m=>{console.error(`DAY013 RUNTIME INTEGRATION FAIL: ${m}`);process.exitCode=1};
const quest=json('docs/experience/kether/day-013/day-013.quest.json');const canon=json('docs/experience/kether/day-013/day-013.canon-blocks.json');const pack=json('docs/experience/kether/day-013/day-013.quest-pack.json');const service=json('docs/experience/kether/day-013/day-013.completion.service.json');
const completion=read('packages/completion-contract/src/day013.ts');const practice=read('packages/practice-contract/src/day013.ts');const client=read('packages/supabase-client/src/day013-v1.ts');const cycle=read('packages/supabase-client/src/kether-cycle03.ts');const library=read('packages/quest-library/src/library.ts');const migration=read('supabase/migrations/20260909204433_day013_completion_contract_v1_reviewed_and_active.sql');const web=read('apps/web/app/day-013/Day013GoldenV1Web.tsx');const webRoute=read('apps/web/app/day-013/page.tsx');const mobile=read('apps/mobile/src/features/kether/Day013GoldenV1Mobile.tsx');const mobileRoute=read('apps/mobile/src/app/day-013.tsx');const webVault=read('apps/web/app/_runtime/WebVaultTextPort.ts');const mobileVault=read('apps/mobile/src/runtime/MobileVaultTextPort.ts');
if(quest.day!==13||canon.source.day!==13)fail('day mismatch');if(canon.counted_words!==705)fail('canon must remain 705 counted words');if(quest.canonical.source_sha!=='b8203e7443af20298679c8f4ca84339e7815438a'||canon.source.blob_sha!==quest.canonical.source_sha)fail('canonical SHA drift');if(quest.canonical.xp!==100)fail('canonical XP drift');
if(quest.progression?.primary_attribute!=='INT'||quest.progression?.secondary_attribute!=='DIS'||quest.progression?.attribute_gain!==1||quest.progression?.client_may_apply!==false||quest.progression?.application!=='SERVER_XP_EVENT_TRIGGER_ONLY')fail('attribute progression contract drift');
if(quest.status!=='TECHNICALLY_ACTIVE__DEVICE_BROWSER_QA_PENDING'||quest.release_blockers?.length!==1||quest.release_blockers[0]?.id!=='DEVICE_BROWSER_QA')fail('quest release state drift');if(pack.release_state!=='TECHNICALLY_ACTIVE__DEVICE_BROWSER_QA_PENDING'||pack.release_blockers?.join(',')!=='DEVICE_BROWSER_QA')fail('pack release state drift');
if(pack.backend?.registry!=='active'||pack.backend?.smoke?.int_before!==5||pack.backend?.smoke?.int_after!==6||pack.backend?.smoke?.per_after!==5||pack.backend?.smoke?.dis_after!==5||pack.backend?.smoke?.attribute_event_code!=='INT'||pack.backend?.smoke?.attribute_event_amount!==1||pack.backend?.smoke?.secondary_attribute_metadata!=='DIS')fail('INT +1 backend smoke proof drift');
if(pack.backend?.smoke?.sitael_completed_days!==3||pack.backend?.smoke?.sitael_lit!==false||pack.backend?.smoke?.fragments_lit!==2)fail('Sitael 3/5 smoke proof drift');if(pack.runtime?.web!=='ACTIVE'||pack.runtime?.expo!=='ACTIVE'||pack.runtime?.vault_e2ee!==true||pack.runtime?.offline_ciphertext_queue!==true||pack.runtime?.authoritative_sitael_assert!==3)fail('runtime proof drift');
if(!completion.includes("deploymentState:'active'")||!completion.includes("primaryAttribute:'INT'")||!completion.includes("secondaryAttribute:'DIS'")||!completion.includes("application:'SERVER_XP_EVENT_TRIGGER'"))fail('completion contract not active');
for(const token of ['last_valid_number','distraction_count','error_count','competition_avoidance_confirmed','relaxed_body_confirmed','load_response','attention_stability_score'])if(!practice.includes(token))fail(`practice evidence field missing ${token}`);for(const token of ['return_strategy_vault_entry_ref','first_error_correction_vault_entry_ref','discipline_error_return_vault_entry_ref','dispersion_error_return_vault_entry_ref'])if(!practice.includes(token))fail(`Vault evidence ref missing ${token}`);if(!practice.includes('(1000-v)%7!==0'))fail('client sequence integrity guard missing');
if(!client.includes('startDay013PracticeSessionV1')||!client.includes('loadDay013PracticeSessionV1')||!client.includes('sealDay013V1'))fail('Supabase client missing');if(!cycle.includes("angel:'Sitael'")||!cycle.includes('expectedLit=expectedCompletedDays===5'))fail('authoritative Sitael guard missing');if(!library.includes('day013QuestJson')||!library.includes('[13,{day:13'))fail('Quest Library Day013 bundle missing');if(!migration.includes("'day013_v1'")||!migration.includes("'HNK-KETHER-D013-COMP-V1'")||!migration.includes("'active'"))fail('active backend migration missing');
if(service.smoke?.result!=='PASS'||service.smoke?.xp_awarded!==100||service.smoke?.xp_after!==1100||service.smoke?.current_day!==14||service.smoke?.int_before!==5||service.smoke?.int_after!==6||service.smoke?.dis_after!==5||service.smoke?.attribute_event_count!==1||service.smoke?.sitael_completed_days!==3||service.smoke?.sitael_lit!==false)fail('completion service smoke proof drift');if(service.validator_tests?.valid!=='PASS'||!String(service.validator_tests?.plaintext_in_jachin).startsWith('REJECTED_')||!String(service.validator_tests?.invalid_sequence).startsWith('REJECTED_')||!String(service.validator_tests?.fractional_score).startsWith('REJECTED_'))fail('validator attack proof missing');
for(const source of [web,mobile]){for(const token of ['flushPending','resolveEntryId','assertSitaelProgress','activeStartedAt','validSequenceNumber','day:13',"kind:'journal'"])if(!source.includes(token))fail(`runtime missing ${token}`);for(const forbidden of ['jachinNote:string','boazNote:string','middleNote:string','soulNote:string'])if(source.includes(forbidden))fail(`plaintext leaked into persisted Draft: ${forbidden}`)}if(!web.includes("document.visibilityState!=='visible'")||!web.includes('visibilitychange'))fail('Web background integrity guard missing');if(!mobile.includes("AppState.addEventListener('change'")||!mobile.includes("state!=='active'"))fail('Expo background integrity guard missing');if(!webRoute.includes('Day013GoldenV1Web'))fail('Web route missing');if(!mobileRoute.includes('AtriumGate')||!mobileRoute.includes('Day013GoldenV1Mobile'))fail('Expo route missing/unguarded');for(const vault of [webVault,mobileVault])if(!vault.includes('flushPending')||!vault.includes('resolveEntryId')||!vault.includes('local-vault:'))fail('offline ciphertext queue capability missing');
if(quest.completion_semantics?.speed_target_required!==false||quest.completion_semantics?.empty_mind_required!==false||quest.completion_semantics?.error_free_run_required!==false||quest.completion_semantics?.exhaustion_required!==false||quest.completion_semantics?.safe_early_stop_valid!==true||quest.completion_semantics?.attribute_gain_client_computed!==false)fail('responsible completion semantics drift');
if(!process.exitCode)console.log('DAY013 RUNTIME INTEGRATION PASS (ACTIVE · Web + Expo · E2EE queue · +100 XP · server INT +1 cap-aware · Sitael 3/5)');
