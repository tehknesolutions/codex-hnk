import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const read=p=>fs.readFileSync(path.join(root,p),'utf8');const json=p=>JSON.parse(read(p));const fail=m=>{console.error(`DAY015 CYCLE SEAL FAIL: ${m}`);process.exitCode=1};
const quest=json('docs/experience/kether/day-015/day-015.quest.json'),canon=json('docs/experience/kether/day-015/day-015.canon-blocks.json'),pack=json('docs/experience/kether/day-015/day-015.quest-pack.json'),service=json('docs/experience/kether/day-015/day-015.completion.service.json');
const completion=read('packages/completion-contract/src/day015.ts'),practice=read('packages/practice-contract/src/day015.ts'),client=read('packages/supabase-client/src/day015-v1.ts'),cycle=read('packages/supabase-client/src/kether-cycle03.ts'),library=read('packages/quest-library/src/library.ts'),migration=read('supabase/migrations/20260909230654_day015_completion_contract_v1_reviewed_and_active.sql'),web=read('apps/web/app/day-015/Day015GoldenV1Web.tsx'),mobile=read('apps/mobile/src/features/kether/Day015GoldenV1Mobile.tsx'),webRoute=read('apps/web/app/day-015/page.tsx'),mobileRoute=read('apps/mobile/src/app/day-015.tsx'),webVault=read('apps/web/app/_runtime/WebVaultTextPort.ts'),mobileVault=read('apps/mobile/src/runtime/MobileVaultTextPort.ts');
if(quest.day!==15||canon.source.day!==15||canon.counted_words!==705||quest.canonical.xp!==150)fail('canonical day/xp/word count drift');if(quest.canonical.source_sha!=='2a5037afce694753bef1b89da89d2746850ef4a1'||canon.source.blob_sha!==quest.canonical.source_sha)fail('canonical SHA drift');
if(quest.progression?.attribute_gain!==0||quest.progression?.application!=='NO_EXECUTABLE_GAIN_RULE'||quest.progression?.client_may_apply!==false)fail('zero attribute contract drift');if(quest.status!=='TECHNICALLY_ACTIVE__DEVICE_BROWSER_QA_PENDING'||quest.release_blockers?.length!==1||quest.release_blockers[0]?.id!=='DEVICE_BROWSER_QA')fail('quest release state drift');if(pack.release_state!=='TECHNICALLY_ACTIVE__DEVICE_BROWSER_QA_PENDING'||pack.release_blockers?.join(',')!=='DEVICE_BROWSER_QA')fail('pack release state drift');
if(pack.backend?.registry!=='active'||pack.backend?.validator!=='day015_v1')fail('backend registry drift');if(pack.runtime?.web!=='ACTIVE'||pack.runtime?.expo!=='ACTIVE'||pack.runtime?.vault_flush_before_seal!==true||pack.runtime?.vault_ref_resolution_before_evidence!==true)fail('runtime integration proof drift');if(pack.safety?.fire_optional!==true||pack.safety?.safe_alternative_equivalent!==true||pack.safety?.burn_requires_full_gate!==true||pack.safety?.non_burn_forbids_burn_safety_payload!==true)fail('fire safety semantics drift');
if(pack.cycle_progress?.expected_after_day!==5||pack.cycle_progress?.fragment!==3||pack.cycle_progress?.expected_lit!==true||pack.cycle_progress?.client_may_light_fragment!==false||pack.cycle_progress?.smoke?.fragments_lit!==3||pack.cycle_progress?.smoke?.current_day!==16||pack.cycle_progress?.smoke?.xp_awarded!==150||pack.cycle_progress?.smoke?.attribute_event_count!==0)fail('Sitael cycle-seal proof drift');
if(!completion.includes("deploymentState:'active'")||!completion.includes('canonicalXp:150')||!completion.includes("NO_EXECUTABLE_GAIN_RULE"))fail('completion contract not active');for(const token of ["Day015Method='BURN_SAFE'|'TEAR'|'SHRED_DISCARD'",'patterns_count:3','symbol_not_guarantee_confirmed:true','responsibility_not_erased_confirmed:true','future_action_committed_not_claimed_completed:true','flame_fully_extinguished_confirmed:true','area_rechecked_confirmed:true'])if(!practice.includes(token))fail(`practice contract missing ${token}`);for(const token of ['externalization_vault_entry_ref','closure_vault_entry_ref','next_cycle_commitment_vault_entry_ref','responsibility_and_tomorrow_action_vault_entry_ref'])if(!practice.includes(token))fail(`Vault evidence ref missing ${token}`);
if(!client.includes('startDay015PracticeSessionV1')||!client.includes('loadDay015PracticeSessionV1')||!client.includes('sealDay015V1'))fail('Supabase client missing');if(!cycle.includes("angel:'Sitael'")||!cycle.includes('expectedLit=expectedCompletedDays===5'))fail('Sitael authoritative guard missing');if(!library.includes('day015QuestJson')||!library.includes('[15,{day:15'))fail('Quest Library Day015 missing');if(!migration.includes("'day015_v1'")||!migration.includes("'HNK-KETHER-D015-COMP-V1'")||!migration.includes("'active'"))fail('active migration missing');
if(service.smoke?.result!=='PASS'||service.smoke?.xp_awarded!==150||service.smoke?.xp_after!==1150||service.smoke?.current_day!==16||service.smoke?.attribute_gain_rule_count!==0||service.smoke?.attribute_event_count!==0||service.smoke?.sitael?.completed_days!==5||service.smoke?.sitael?.lit!==true||service.smoke?.fragments_lit!==3||service.smoke?.progression_events?.join(',')!=='NEXT_DAY_UNLOCKED,KETHER_FRAGMENT_LIT')fail('rollback smoke proof drift');if(service.validator_tests?.tear_valid!=='PASS'||service.validator_tests?.burn_safe_valid!=='PASS'||!String(service.validator_tests?.plaintext_in_jachin).startsWith('REJECTED_')||!String(service.validator_tests?.burn_without_safety).startsWith('REJECTED_')||!String(service.validator_tests?.non_burn_with_burn_safety).startsWith('REJECTED_'))fail('validator attack proof missing');
for(const source of [web,mobile]){for(const token of ['flushPending','resolveEntryId','assertSitaelProgress','BURN_SAFE','TEAR','SHRED_DISCARD','symbolNotGuarantee','responsibilityNotErased',"day:15", "kind:'journal'"])if(!source.includes(token))fail(`runtime missing ${token}`);for(const forbidden of ['pattern1:string','pattern2:string','pattern3:string','closureNote:string','commitmentNote:string','soulNote:string'])if(source.includes(forbidden))fail(`plaintext field leaked into Draft contract ${forbidden}`)}
if(!webRoute.includes('Day015GoldenV1Web'))fail('Web route missing');if(!mobileRoute.includes('AtriumGate')||!mobileRoute.includes('Day015GoldenV1Mobile'))fail('Expo route missing or unguarded');for(const v of [webVault,mobileVault])if(!v.includes('flushPending')||!v.includes('resolveEntryId')||!v.includes('local-vault:'))fail('offline ciphertext queue missing');if(quest.completion_semantics?.fire_required!==false||quest.completion_semantics?.safe_alternative_equivalent!==true||quest.completion_semantics?.symbolic_destruction_solves_problem!==false||quest.completion_semantics?.future_action_must_already_be_completed!==false||quest.completion_semantics?.responsibility_remains!==true||quest.completion_semantics?.sitael_fragment_client_computed!==false)fail('responsible cycle-seal semantics drift');
if(!process.exitCode)console.log('DAY015 CYCLE SEAL PASS (ACTIVE · Web + Expo · fire optional · E2EE queue · +150 XP · +0 attribute · Sitael 5/5 · fragment III lit · Crown 3/7)');
