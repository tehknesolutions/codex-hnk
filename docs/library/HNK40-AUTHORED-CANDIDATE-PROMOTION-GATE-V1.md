# HNK-40 Authored Candidate Promotion Gate V1

This gate asks one narrow question: whether the **exact compact relation set** recovered from the HNK Oraculum RC1 Appendix B should move from `HNK_AUTHORED_CANDIDATE` to `HNK_APPROVED`.

Scope: G01–G40 fields `world_candidate`, `role`, `geometry`, `sigil`, `light`, and `shadow`.

Approval does **not** change phoneme, transliteration, visual form, lexicon, grammar, reference-language mappings, or the HNK-40/HENUVOKODAN boundary. The longer Transformation/Question/Action prose remains candidate source material unless separately promoted.

The gate is ready for explicit creator decision. Accepted approval tokens: `A.S.`, `AS`, or `APROVADO. SIGA.`
