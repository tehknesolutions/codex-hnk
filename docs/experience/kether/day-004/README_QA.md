# Day 004 — QA Boundary

Day 004 is technically active. Canon, Quest, generic `EXPERIMENT`, Web/Expo routes, typed Evidence, Completion V2 and the server smoke are implemented.

Still required before final release labeling:
- real browser flow on `/day-004`;
- real Expo device flow on `/day-004`;
- audio start/pause/stop with the Theta/432 profile;
- Safety Stop during visualization, monitoring, audio and experiment;
- verify that `NO_NOTICEABLE_CHANGE` is understandable and completable in UX;
- private observation/trigger copy review against Vault behavior;
- authenticated seal in preview environment;
- two-connection concurrency test.

These QA items do not authorize client-side XP, attribute gain, causal claims, or changes to Canon.
