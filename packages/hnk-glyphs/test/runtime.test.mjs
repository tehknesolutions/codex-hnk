import test from 'node:test'; import assert from 'node:assert/strict'; import {createHash} from 'node:crypto';
import {HNK40_ENTRIES,HNK40_GLYPH_IDS,HNK40_IPA,HNK40_CANDIDATE_PUA_RANGE,HNK40_SPRITE_SHA256,HNK40_GLYPH_SET_SHA256,HNK40_SPRITE_SVG,HNK40_SACRED_10X4,glyphIdFromNumber,getSacredKey,transliterationToGlyphIds,transliterationToCandidatePuaText,candidatePuaTextToGlyphIds,getGlyphSvg,validateHnk40Runtime} from '../src/index.mjs';
const IPA=["/a/", "/e/", "/i/", "/o/", "/u/", "/ə/", "/h/", "/ʔ/", "/ʕ/", "/ħ/", "/m/", "/n/", "/ŋ/", "/l/", "/r/", "/j/", "/w/", "/b/", "/d/", "/g/", "/p/", "/t/", "/k/", "/q/", "/f/", "/s/", "/ʃ/", "/x/", "/θ/", "/ts/", "/v/", "/z/", "/ʒ/", "/ð/", "/tʃ/", "/dʒ/", "/sˤ/", "/tˤ/", "/ɣ/", "/y/"];
test('40 IDs + IPA',()=>{assert.equal(HNK40_ENTRIES.length,40);assert.deepEqual(HNK40_GLYPH_IDS,Array.from({length:40},(_,i)=>glyphIdFromNumber(i+1)));assert.deepEqual(HNK40_IPA,IPA)});
test('PUA isolated',()=>{assert.deepEqual(HNK40_CANDIDATE_PUA_RANGE,{start:0xE100,end:0xE127,count:40});assert.deepEqual(HNK40_ENTRIES.map(e=>e.candidatePuaDecimal),Array.from({length:40},(_,i)=>0xE100+i));assert.ok(HNK40_ENTRIES.every(e=>e.candidatePuaDecimal>0xE00B))});
test('Sacred 10x4 bijective',()=>{assert.equal(HNK40_SACRED_10X4.length,4);assert.ok(HNK40_SACRED_10X4.every(r=>r.length===10));assert.equal(getSacredKey(1,1).glyphId,'G01');assert.equal(getSacredKey(4,10).glyphId,'G40')});
test('translit exact + TS atomic',()=>{assert.deepEqual(transliterationToGlyphIds('VAMAKALA',{strict:true}).glyphIds,['G31','G01','G11','G01','G23','G01','G14','G01']);assert.deepEqual(transliterationToGlyphIds('PITSA',{strict:true}).glyphIds,['G21','G03','G30','G01']);assert.throws(()=>transliterationToGlyphIds('QA',{strict:true}),/Unresolved/)});
test('PUA roundtrip',()=>{const a=transliterationToCandidatePuaText('HENUVOKODAN',{strict:true});assert.deepEqual(candidatePuaTextToGlyphIds(a.text).glyphIds,a.glyphIds)});
test('inline SVG self contained',()=>{for(const gid of HNK40_GLYPH_IDS){const svg=getGlyphSvg(gid);assert.match(svg,/^<svg /);assert.ok(!svg.includes('<use '),gid)}});
test('freeze checksums',()=>{assert.equal(createHash('sha256').update(HNK40_SPRITE_SVG).digest('hex'),HNK40_SPRITE_SHA256);assert.equal(HNK40_GLYPH_SET_SHA256,"78668df0f707952b7c280de52526abaa2b7900597fb8ff362a3a08641fd1bce5")});
test('runtime self check',()=>assert.deepEqual(validateHnk40Runtime(),{ok:true,errors:[]}));
