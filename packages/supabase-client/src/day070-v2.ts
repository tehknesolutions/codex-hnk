import type {HnkSupabaseClient} from './index';
import {DAY070_MASTER_PATH,DAY070_MASTER_SHA256,type Day070ReferenceGate} from '@hnk/practice-contract';
export async function loadDay070ReferenceGate(client:HnkSupabaseClient):Promise<Day070ReferenceGate>{const{data,error}=await client.from('asset_registry').select('asset_key,approval_state,checksum_sha256,metadata').eq('asset_key','hnk.tetragrammaton.day070.v1.master').maybeSingle();if(error)throw error;if(!data)return{canonicalReferenceReady:false,assetKey:null,approvalState:null,checksumSha256:null,repoPath:null,upright:false,mirrored:false};const meta=(data.metadata&&typeof data.metadata==='object'&&!Array.isArray(data.metadata)?data.metadata:{}) as Record<string,unknown>;const repoPath=typeof meta.repo_path==='string'?meta.repo_path:null,upright=meta.upright===true,mirrored=meta.mirrored===true;return{canonicalReferenceReady:data.approval_state==='approved'&&data.checksum_sha256===DAY070_MASTER_SHA256&&repoPath===DAY070_MASTER_PATH&&upright&&!mirrored,assetKey:data.asset_key,approvalState:data.approval_state,checksumSha256:data.checksum_sha256,repoPath,upright,mirrored}}
export async function sealDay070V2():Promise<never>{throw new Error('day070_reference_locked')}
