'use client';
import {useEffect,useMemo,useRef,useState,type CSSProperties,type Dispatch,type SetStateAction} from 'react';
import {ExperienceDirector,QuestRuntime,createQuestCatalog,type ExperienceDirective,type PlayerContext,type SessionSnapshot} from '@hnk/quest-engine';
import {createBundledQuestLibrary,type RuntimeQuestBundle} from '@hnk/quest-library';
import {assertLelahelProgress,loadDay028PracticeSessionV1,sealDay028V1,startDay028PracticeSessionV1} from '@hnk/supabase-client';
import {createWebQuestSnapshotStore} from '../_runtime/quest-snapshot-store';
import {createWebVaultTextPort} from '../_runtime/WebVaultTextPort';
import {useWebHnkRuntime} from '../_runtime/WebHnkRuntime';

type Session=NonNullable<Awaited<ReturnType<typeof loadDay028PracticeSessionV1>>>;type Seal=Extract<Awaited<ReturnType<typeof sealDay028V1>>,{ok:true}>['response'];type Timer='ACTIVE'|'CONTROL'|'SILENCE';
interface Draft{activeDuration:number;postureStable:boolean;eyesClosed:boolean;eightMapped:boolean;centerBetween:boolean;activeConstruction:number;activeLosses:number;stableCircuit:number;difficultCircuit:number;returnSeconds:number;activeStability:number;activeNavigation:number;activeRef:string|null;controlDuration:number;circleEight:boolean;sameFunctions:boolean;sameOrder:boolean;sameDuration:boolean;samePosture:boolean;noAstralNames:boolean;noFormula:boolean;controlNotSabotaged:boolean;controlConstruction:number;controlPositionErrors:number;controlLosses:number;controlRecovery:number;controlStability:number;controlNavigation:number;controlPresence:number;dataCorrectExpectation:boolean;controlRef:string|null;middleSequenceDuration:number;middleCircuits:number;middleBreaths:number;middleCenterReturns:number;middleSilent:number;middleReconstruction:number;lamed:boolean;psalm911:boolean;middleNaturalBreathing:boolean;identityOrientation:boolean;noExcessiveTension:boolean;e1e5:boolean;discriminableCircuit:number;internalProtocolBoundary:boolean;singleSessionBoundary:boolean;hypothesisRefined:boolean;stabilityCriterion:boolean;integrationRef:string|null;soulRef:string|null;safetyStopped:boolean}
const empty:Draft={activeDuration:0,postureStable:false,eyesClosed:false,eightMapped:false,centerBetween:false,activeConstruction:0,activeLosses:0,stableCircuit:1,difficultCircuit:1,returnSeconds:0,activeStability:5,activeNavigation:5,activeRef:null,controlDuration:0,circleEight:false,sameFunctions:false,sameOrder:false,sameDuration:false,samePosture:false,noAstralNames:false,noFormula:false,controlNotSabotaged:false,controlConstruction:0,controlPositionErrors:0,controlLosses:0,controlRecovery:0,controlStability:5,controlNavigation:5,controlPresence:5,dataCorrectExpectation:false,controlRef:null,middleSequenceDuration:0,middleCircuits:0,middleBreaths:0,middleCenterReturns:0,middleSilent:0,middleReconstruction:0,lamed:false,psalm911:false,middleNaturalBreathing:false,identityOrientation:false,noExcessiveTension:false,e1e5:false,discriminableCircuit:1,internalProtocolBoundary:false,singleSessionBoundary:false,hypothesisRefined:false,stabilityCriterion:false,integrationRef:null,soulRef:null,safetyStopped:false};
function canon(b:RuntimeQuestBundle,d:ExperienceDirective){return(d.phase.source.block_ids??[]).map(id=>{const x=b.canon.blocks.find(v=>v.id===id);if(!x)throw new Error(`canonical_block_missing:${id}`);return x.text})}function keys(u:string){return{snapshot:`hnk:quest:d028:${u}:snapshot:v1`,session:`hnk:quest:d028:${u}:session:v1`,cache:`hnk:quest:d028:${u}:session-cache:v1`,draft:`hnk:quest:d028:${u}:draft:v1`}}function parse<T>(k:string):T|null{try{const x=localStorage.getItem(k);return x?JSON.parse(x) as T:null}catch{return null}}function clientId(u:string){return`hnk-web-d028-v1-${u}-${Date.now()}-${Math.random().toString(36).slice(2,8)}`}function clock(e:number,m:number){const l=Math.max(0,m-e);return`${String(Math.floor(l/60)).padStart(2,'0')}:${String(l%60).padStart(2,'0')}`}
export function Day028GoldenV1Web(){const auth=useWebHnkRuntime();const live=Boolean(auth.configured&&auth.phase==='signed-in'&&auth.client&&auth.userId);const runtimeRef=useRef<QuestRuntime|null>(null);const[bundle,setBundle]=useState<RuntimeQuestBundle|null>(null);const[snapshot,setSnapshot]=useState<SessionSnapshot|null>(null);const[session,setSession]=useState<Session|null>(null);const[draft,setDraft]=useState<Draft>(empty);const[loaded,setLoaded]=useState(false);const[timer,setTimer]=useState<Timer|null>(null);const[started,setStarted]=useState<number|null>(null);const[now,setNow]=useState(Date.now());const[middleCircuit,setMiddleCircuit]=useState<number|null>(null);const[middleStarted,setMiddleStarted]=useState<number|null>(null);const[activeNote,setActiveNote]=useState('');const[controlNote,setControlNote]=useState('');const[integrationNote,setIntegrationNote]=useState('');const[soulNote,setSoulNote]=useState('');const[voluntary,setVoluntary]=useState(false);const[error,setError]=useState<string|null>(null);const[busy,setBusy]=useState(false);const[sealed,setSealed]=useState<Seal|null>(null);const vault=useMemo(()=>auth.userId?createWebVaultTextPort(auth.client??null):null,[auth.client,auth.userId]);const context=useMemo<PlayerContext>(()=>({mediationMode:'HNK_CANONICAL',accessibility:{reducedMotion:window.matchMedia?.('(prefers-reduced-motion: reduce)').matches===true,audioEnabled:false,microphoneAvailable:false},offline:!live}),[live]);
useEffect(()=>{if(!auth.userId)return;let ok=true;const lib=createBundledQuestLibrary(),cat=createQuestCatalog(lib),store=createWebQuestSnapshotStore(),k=keys(auth.userId);void Promise.all([cat.requireDay(28),lib.loadBundle(28),store.load(k.snapshot)]).then(([def,b,saved])=>{if(!ok||!b)return;const r=new QuestRuntime(def);runtimeRef.current=r;setBundle(b);setSnapshot(saved?r.restore(saved):r.start());setDraft(parse<Draft>(k.draft)??empty);const c=parse<Session>(k.cache);if(c)setSession(c);setLoaded(true)}).catch(e=>ok&&setError(e instanceof Error?e.message:'day028_load_failed'));return()=>{ok=false}},[auth.userId]);
useEffect(()=>{if(!auth.userId||!snapshot)return;const k=keys(auth.userId),store=createWebQuestSnapshotStore();if(snapshot.runState==='COMPLETE'){void store.remove(k.snapshot);localStorage.removeItem(k.draft);localStorage.removeItem(k.session);localStorage.removeItem(k.cache)}else void store.save(k.snapshot,snapshot)},[auth.userId,snapshot]);useEffect(()=>{if(auth.userId&&loaded&&snapshot?.runState!=='COMPLETE')localStorage.setItem(keys(auth.userId).draft,JSON.stringify(draft))},[auth.userId,draft,loaded,snapshot?.runState]);useEffect(()=>{const id=window.setInterval(()=>setNow(Date.now()),500);return()=>window.clearInterval(id)},[]);useEffect(()=>{if(started===null||timer===null)return;const e=Math.floor((now-started)/1000);if(timer==='ACTIVE'&&e>=600){setDraft(d=>({...d,activeDuration:600}));setTimer(null);setStarted(null)}else if(timer==='CONTROL'&&e>=600){setDraft(d=>({...d,controlDuration:600}));setTimer(null);setStarted(null)}else if(timer==='SILENCE'&&e>=60){setDraft(d=>({...d,middleSilent:60}));setTimer(null);setStarted(null)}},[timer,started,now]);
useEffect(()=>{const h=()=>{if(document.visibilityState==='visible')return;if(started!==null||middleStarted!==null){setDraft(d=>timer==='ACTIVE'?{...d,activeDuration:0}:timer==='CONTROL'?{...d,controlDuration:0}:{...d,middleSequenceDuration:0,middleCircuits:0,middleBreaths:0,middleCenterReturns:0,middleSilent:0});setTimer(null);setStarted(null);setMiddleCircuit(null);setMiddleStarted(null);setError('bloco_interrompido__sequencia_corrente_descartada')}};document.addEventListener('visibilitychange',h);return()=>document.removeEventListener('visibilitychange',h)},[started,middleStarted,timer]);useEffect(()=>{if(!live||!auth.client||!auth.userId)return;const k=keys(auth.userId),id=localStorage.getItem(k.session);if(!id)return;void loadDay028PracticeSessionV1(auth.client,id).then(s=>{if(s){setSession(s);localStorage.setItem(k.cache,JSON.stringify(s))}}).catch(()=>undefined)},[auth.client,auth.userId,live]);
const director=useMemo(()=>bundle?new ExperienceDirector(bundle.quest):null,[bundle]);const directive=useMemo(()=>director&&snapshot?.currentPhaseId?director.resolvePhase(snapshot.currentPhaseId,context,snapshot):null,[director,context,snapshot]);const max=timer==='SILENCE'?60:600,elapsed=started===null?0:Math.min(max,Math.max(0,Math.floor((now-started)/1000)));
async function ensureSession(){if(session)return session;if(!live||!auth.client||!auth.userId)throw new Error('online_session_required_before_day028_practice');const s=await startDay028PracticeSessionV1(auth.client,{clientSessionId:clientId(auth.userId),appVersion:'0.1.0-web-day028-golden-v1'});setSession(s);const k=keys(auth.userId);localStorage.setItem(k.session,s.id);localStorage.setItem(k.cache,JSON.stringify(s));return s}async function complete(id:string){setError(null);try{if(id==='jachin_reading')await ensureSession();if(runtimeRef.current)setSnapshot(runtimeRef.current.completePhase(id));window.scrollTo({top:0,behavior:'smooth'})}catch(e){setError(e instanceof Error?e.message:'phase_failed')}}function startTimer(t:Timer){if(started!==null||middleStarted!==null)return;setTimer(t);setStarted(Date.now());setNow(Date.now());setError(null)}function resetMiddle(){setMiddleCircuit(null);setMiddleStarted(null);setTimer(null);setStarted(null);setDraft(d=>({...d,middleSequenceDuration:0,middleCircuits:0,middleBreaths:0,middleCenterReturns:0,middleSilent:0}))}function startMiddle(){if(started!==null||middleStarted!==null)return;resetMiddle();setMiddleCircuit(1);setMiddleStarted(Date.now());setError(null)}function confirmCircuit(){if(middleCircuit===null||middleStarted===null)return;if(middleCircuit<8){setMiddleCircuit(middleCircuit+1);return}const duration=Math.max(1,Math.floor((Date.now()-middleStarted)/1000));setDraft(d=>({...d,middleSequenceDuration:duration,middleCircuits:8,middleBreaths:3,middleCenterReturns:8,middleSilent:0}));setMiddleCircuit(null);setMiddleStarted(null);setTimer('SILENCE');setStarted(Date.now());setNow(Date.now())}function safetyStop(){if(timer==='ACTIVE')setDraft(d=>({...d,activeDuration:0,safetyStopped:true}));else if(timer==='CONTROL')setDraft(d=>({...d,controlDuration:0,safetyStopped:true}));else setDraft(d=>({...d,middleSequenceDuration:0,middleCircuits:0,middleBreaths:0,middleCenterReturns:0,middleSilent:0,safetyStopped:true}));setTimer(null);setStarted(null);setMiddleCircuit(null);setMiddleStarted(null);setError('safety_stop__reoriente_se__retome_somente_com_clareza_e_conforto');if(runtimeRef.current)setSnapshot(runtimeRef.current.safetyStop())}function resume(){setError(null);if(runtimeRef.current)setSnapshot(runtimeRef.current.resume())}
async function savePrivate(kind:'active'|'control'|'integration'|'soul'){if(!vault||!auth.userId)throw new Error('vault_unavailable');const text=kind==='active'?activeNote:kind==='control'?controlNote:kind==='integration'?integrationNote:soulNote;if(!text.trim())throw new Error(`${kind}_private_note_required`);const e=await vault.saveText({userId:auth.userId,day:28,kind:'journal',plaintext:JSON.stringify({schema:`hnk-day028-${kind}-v1`,text:text.trim()})});if(kind==='active'){setDraft(d=>({...d,activeRef:e.entryId}));setActiveNote('');await complete('jachin_gneo_geo')}else if(kind==='control'){setDraft(d=>({...d,controlRef:e.entryId}));setControlNote('');await complete('boaz_simple_geometry_control')}else if(kind==='integration'){setDraft(d=>({...d,integrationRef:e.entryId}));setIntegrationNote('');await complete('middle_gneo_geo')}else{setDraft(d=>({...d,soulRef:e.entryId}));setSoulNote('');await complete('soul_mirror')}}
async function saveActive(){if(draft.activeDuration!==600||!draft.postureStable||!draft.eyesClosed||!draft.eightMapped||!draft.centerBetween)throw new Error('day028_jachin_requirements');await savePrivate('active')}async function saveControl(){if(draft.controlDuration!==600||!draft.circleEight||!draft.sameFunctions||!draft.sameOrder||!draft.sameDuration||!draft.samePosture||!draft.noAstralNames||!draft.noFormula||!draft.controlNotSabotaged||!draft.dataCorrectExpectation)throw new Error('day028_boaz_requirements');await savePrivate('control')}async function saveIntegration(){if(draft.middleSequenceDuration<1||draft.middleCircuits!==8||draft.middleBreaths!==3||draft.middleCenterReturns!==8||draft.middleSilent!==60||!draft.lamed||!draft.psalm911||!draft.middleNaturalBreathing||!draft.identityOrientation||!draft.noExcessiveTension||!draft.e1e5||!draft.internalProtocolBoundary||!draft.singleSessionBoundary||!draft.hypothesisRefined||!draft.stabilityCriterion)throw new Error('day028_middle_requirements');await savePrivate('integration')}
async function seal(){if(!runtimeRef.current||!live||!auth.client||!auth.userId||!session||!vault||!draft.activeRef||!draft.controlRef||!draft.integrationRef||!draft.soulRef||!voluntary)return;setBusy(true);setError(null);try{await vault.flushPending(auth.userId);const[a,b,c,d]=await Promise.all([draft.activeRef,draft.controlRef,draft.integrationRef,draft.soulRef].map(x=>vault.resolveEntryId(auth.userId!,x)));if(!a||!b||!c||!d)throw new Error('vault_sync_required_before_completion');const result=await sealDay028V1(auth.client,{evidence:{sessionId:session.id,activeDurationSeconds:draft.activeDuration,postureStableConfirmed:draft.postureStable,eyesClosedConfirmed:draft.eyesClosed,eightCircuitsMappedConfirmed:draft.eightMapped,returnToCenterBetweenCircuitsConfirmed:draft.centerBetween,activeConstructionTimeSeconds:draft.activeConstruction,activeOrientationLossCount:draft.activeLosses,mostStableCircuit:draft.stableCircuit,mostDifficultCircuit:draft.difficultCircuit,averageReturnToCenterSeconds:draft.returnSeconds,activeSpatialStabilityScore:draft.activeStability,activeNavigationScore:draft.activeNavigation,activeVaultEntryRef:a,controlDurationSeconds:draft.controlDuration,circlePlusEightPointsConfirmed:draft.circleEight,sameFunctionsConfirmed:draft.sameFunctions,sameOrderConfirmed:draft.sameOrder,sameDurationConfirmed:draft.sameDuration,samePostureConfirmed:draft.samePosture,noAstralNomenclatureConfirmed:draft.noAstralNames,noFormulaConfirmed:draft.noFormula,controlNotSabotagedConfirmed:draft.controlNotSabotaged,controlConstructionTimeSeconds:draft.controlConstruction,controlPositionErrorCount:draft.controlPositionErrors,controlOrientationLossCount:draft.controlLosses,controlAverageRecoverySeconds:draft.controlRecovery,controlSpatialStabilityScore:draft.controlStability,controlNavigationScore:draft.controlNavigation,controlPresenceScore:draft.controlPresence,dataMayCorrectExpectationConfirmed:draft.dataCorrectExpectation,controlVaultEntryRef:b,middleSequenceDurationSeconds:draft.middleSequenceDuration,middleCircuitsCompleted:draft.middleCircuits,middleBreathsPerCircuit:draft.middleBreaths,middleCenterReturns:draft.middleCenterReturns,middleSilentCenterSeconds:draft.middleSilent,middleFinalReconstructionTimeSeconds:draft.middleReconstruction,lamedLamedHeOnceConfirmed:draft.lamed,psalm911OrientationConfirmed:draft.psalm911,middleNaturalBreathingConfirmed:draft.middleNaturalBreathing,identityOrientationPreservedConfirmed:draft.identityOrientation,noExcessiveTensionConfirmed:draft.noExcessiveTension,e1E5RecordedConfirmed:draft.e1e5,mostDiscriminableCircuit:draft.discriminableCircuit,cockpitIsInternalProtocolNotExternalRealmProofConfirmed:draft.internalProtocolBoundary,singleSessionNotDefinitiveConfirmed:draft.singleSessionBoundary,hypothesisRefinedConfirmed:draft.hypothesisRefined,stabilityCriterionDefinedConfirmed:draft.stabilityCriterion,integrationVaultEntryRef:c,soulMirrorCompleted:true,centerControlIndicatorVaultEntryRef:d,safetyStopOccurred:draft.safetyStopped}});if(!result.ok)throw new Error(result.code);assertLelahelProgress(result.response.crown,3);setSealed(result.response);setSnapshot(runtimeRef.current.confirmServerCompletion())}catch(e){setError(e instanceof Error?e.message:'day028_completion_failed')}finally{setBusy(false)}}
if(!auth.userId)return <Center text="ENTRE NO ÁTRIO PARA INICIAR O DIA 028."/>;if(!bundle||!snapshot||!director)return <Center text={error??'CARREGANDO DAY 028…'}/>;if(snapshot.runState==='SAFETY_STOP')return <main style={styles.center}><h1>PRÁTICA INTERROMPIDA</h1><p>Reoriente-se. Retome apenas com identidade, orientação e conforto preservados.</p><button onClick={resume}>RETOMAR</button></main>;if(snapshot.runState==='COMPLETE')return <main style={styles.center}><small>DIA 028 · LELAHEL 3/5</small><h1>GNEO GEO REGISTRADO</h1><p>{sealed?.first_completion?`+${sealed.xp_awarded} XP canônicos.`:'Revisita sem novo XP.'}</p><p>INT é aplicado somente pelo servidor. PER permanece secundário. Fragmento VI apagado · Coroa {sealed?.crown.fragments_lit??5}/7.</p><p>O cockpit não precisa superar o controle para validar a prática.</p></main>;if(!directive)return <Center text="FASE INDISPONÍVEL."/>;const p=directive.phase,texts=directive.requiresCanonicalContent?canon(bundle,directive):[];
return <main style={styles.shell}><small>DIA 028 · KETHER · LELAHEL 3/5</small><h1>GNEO GEO ASTRAL · COCKPIT × CONTROLE</h1><p>O cockpit é uma arquitetura interna do protocolo HNK. Visualização, presença ou sensação de comando não provam acesso a um reino externo.</p>{error?<p style={styles.error}>{error}</p>:null}<section style={styles.panel}><small>{p.type} · {p.id}</small>{p.type==='NARRATIVE'?<><h2>Arquitetura antes de interpretação.</h2><p>O controle simples pode ser mais rápido, estável ou navegável e continua sendo dado válido.</p></>:null}{p.type==='READ'?texts.map((t,i)=><p key={i}>{t}</p>):null}{p.id==='jachin_gneo_geo'?<><TimerBox label="COCKPIT ATIVO" value={timer==='ACTIVE'?clock(elapsed,600):draft.activeDuration===600?'CONCLUÍDO':'10:00'} disabled={timer!==null||middleStarted!==null||draft.activeDuration===600} onStart={()=>startTimer('ACTIVE')}/><Checks items={[['Postura estável','postureStable'],['Olhos fechados','eyesClosed'],['Oito circuitos mapeados','eightMapped'],['Retorno ao centro entre circuitos','centerBetween']]} draft={draft} setDraft={setDraft}/><Metrics kind="active" draft={draft} setDraft={setDraft}/><textarea value={activeNote} onChange={e=>setActiveNote(e.target.value)} placeholder="Registro privado do mapa, funções e fenômenos percebidos."/><button disabled={draft.activeDuration!==600} onClick={()=>void saveActive()}>CIFRAR ATIVO</button></>:null}{p.id==='boaz_simple_geometry_control'?<><TimerBox label="CONTROLE · CÍRCULO + 8 PONTOS" value={timer==='CONTROL'?clock(elapsed,600):draft.controlDuration===600?'CONCLUÍDO':'10:00'} disabled={timer!==null||middleStarted!==null||draft.controlDuration===600} onStart={()=>startTimer('CONTROL')}/><Checks items={[['Círculo + oito pontos','circleEight'],['Mesmas funções','sameFunctions'],['Mesma ordem','sameOrder'],['Mesma duração','sameDuration'],['Mesma postura','samePosture'],['Sem nomenclatura astral','noAstralNames'],['Sem fórmula','noFormula'],['Não sabotei o controle','controlNotSabotaged'],['Dados podem corrigir expectativa','dataCorrectExpectation']]} draft={draft} setDraft={setDraft}/><Metrics kind="control" draft={draft} setDraft={setDraft}/><textarea value={controlNote} onChange={e=>setControlNote(e.target.value)} placeholder="Registro privado do controle e variáveis concorrentes."/><button disabled={draft.controlDuration!==600} onClick={()=>void saveControl()}>CIFRAR CONTROLE</button></>:null}{p.id==='middle_gneo_geo'?<><div style={styles.guide}><h3>SEQUÊNCIA GUIADA · SEM META DE VELOCIDADE</h3>{middleCircuit===null&&draft.middleSilent!==60?<button onClick={startMiddle}>INICIAR 8 CIRCUITOS</button>:null}{middleCircuit!==null?<><strong>CIRCUITO {middleCircuit}/8</strong><p>Permaneça por 3 respirações naturais. Depois retorne conscientemente ao centro.</p><button onClick={confirmCircuit}>CONFIRMAR 3 RESPIRAÇÕES + RETORNO AO CENTRO</button></>:null}{timer==='SILENCE'?<strong>CENTRO EM SILÊNCIO · {clock(elapsed,60)}</strong>:null}{draft.middleSilent===60?<p>Sequência concluída em {draft.middleSequenceDuration}s + 60s no centro.</p>:null}</div><Checks items={[['LAMED-LAMED-HE uma vez','lamed'],['Salmo 9:11','psalm911'],['Respiração natural','middleNaturalBreathing'],['Identidade e orientação preservadas','identityOrientation'],['Sem tensão excessiva','noExcessiveTension'],['E1–E5 registrados','e1e5'],['Cockpit interno ≠ prova de reino externo','internalProtocolBoundary'],['Uma sessão não é definitiva','singleSessionBoundary'],['Hipótese refinada','hypothesisRefined'],['Critério de estabilidade definido','stabilityCriterion']]} draft={draft} setDraft={setDraft}/><label>Reconstrução final (s)<input type="number" min={0} value={draft.middleReconstruction} onChange={e=>setDraft(d=>({...d,middleReconstruction:Number(e.target.value)}))}/></label><label>Circuito mais discriminável <input type="number" min={1} max={8} value={draft.discriminableCircuit} onChange={e=>setDraft(d=>({...d,discriminableCircuit:Number(e.target.value)}))}/></label><textarea value={integrationNote} onChange={e=>setIntegrationNote(e.target.value)} placeholder="Registro privado E1–E5, transições e retorno ao centro."/><button disabled={draft.middleSilent!==60} onClick={()=>void saveIntegration()}>CIFRAR INTEGRAÇÃO</button></>:null}{p.id==='soul_mirror'?<><textarea value={soulNote} onChange={e=>setSoulNote(e.target.value)} placeholder="Onde perdi o centro? O que mudou contra o controle?"/><button onClick={()=>void savePrivate('soul')}>CIFRAR ESPELHO</button></>:null}{p.id==='completion'?<><label><input type="checkbox" checked={voluntary} onChange={e=>setVoluntary(e.target.checked)}/> Concluo voluntariamente; nenhum resultado de presença ou superioridade é exigido.</label><button disabled={!voluntary||busy} onClick={()=>void seal()}>{busy?'SELANDO…':'SELAR DAY 028'}</button></>:null}{!['jachin_gneo_geo','boaz_simple_geometry_control','middle_gneo_geo','soul_mirror','completion'].includes(p.id)?<button onClick={()=>void complete(p.id)}>CONTINUAR</button>:null}</section><button style={styles.stop} onClick={safetyStop}>SAFETY STOP</button></main>}
function Metrics({kind,draft,setDraft}:{kind:'active'|'control';draft:Draft;setDraft:Dispatch<SetStateAction<Draft>>}){if(kind==='active')return <div style={styles.grid}><Num label="Construção (s)" v={draft.activeConstruction} max={600} set={v=>setDraft(d=>({...d,activeConstruction:v}))}/><Num label="Perdas de orientação" v={draft.activeLosses} max={9999} set={v=>setDraft(d=>({...d,activeLosses:v}))}/><Num label="Circuito mais estável" v={draft.stableCircuit} min={1} max={8} set={v=>setDraft(d=>({...d,stableCircuit:v}))}/><Num label="Circuito mais difícil" v={draft.difficultCircuit} min={1} max={8} set={v=>setDraft(d=>({...d,difficultCircuit:v}))}/><Num label="Retorno médio ao centro (s)" v={draft.returnSeconds} max={600} set={v=>setDraft(d=>({...d,returnSeconds:v}))}/><Num label="Estabilidade 0–10" v={draft.activeStability} max={10} set={v=>setDraft(d=>({...d,activeStability:v}))}/><Num label="Navegação 0–10" v={draft.activeNavigation} max={10} set={v=>setDraft(d=>({...d,activeNavigation:v}))}/></div>;return <div style={styles.grid}><Num label="Construção (s)" v={draft.controlConstruction} max={600} set={v=>setDraft(d=>({...d,controlConstruction:v}))}/><Num label="Erros de posição" v={draft.controlPositionErrors} max={9999} set={v=>setDraft(d=>({...d,controlPositionErrors:v}))}/><Num label="Perdas de orientação" v={draft.controlLosses} max={9999} set={v=>setDraft(d=>({...d,controlLosses:v}))}/><Num label="Recuperação média (s)" v={draft.controlRecovery} max={600} set={v=>setDraft(d=>({...d,controlRecovery:v}))}/><Num label="Estabilidade 0–10" v={draft.controlStability} max={10} set={v=>setDraft(d=>({...d,controlStability:v}))}/><Num label="Navegação 0–10" v={draft.controlNavigation} max={10} set={v=>setDraft(d=>({...d,controlNavigation:v}))}/><Num label="Presença 0–10" v={draft.controlPresence} max={10} set={v=>setDraft(d=>({...d,controlPresence:v}))}/></div>}
function Num({label,v,set,min=0,max}:{label:string;v:number;set:(n:number)=>void;min?:number;max:number}){return <label>{label}<input type="number" min={min} max={max} value={v} onChange={e=>set(Number(e.target.value))}/></label>}function Checks({items,draft,setDraft}:{items:Array<[string,keyof Draft]>;draft:Draft;setDraft:Dispatch<SetStateAction<Draft>>}){return <div>{items.map(([label,key])=><label key={String(key)} style={styles.check}><input type="checkbox" checked={Boolean(draft[key])} onChange={e=>setDraft(d=>({...d,[key]:e.target.checked}))}/>{label}</label>)}</div>}function TimerBox({label,value,onStart,disabled}:{label:string;value:string;onStart:()=>void;disabled:boolean}){return <div style={styles.timer}><b>{label}</b><strong>{value}</strong><button disabled={disabled} onClick={onStart}>INICIAR</button></div>}function Center({text}:{text:string}){return <main style={styles.center}><p>{text}</p></main>}
const styles:Record<string,CSSProperties>={shell:{maxWidth:920,margin:'0 auto',padding:'32px 20px 90px',fontFamily:'system-ui',lineHeight:1.55},panel:{display:'grid',gap:16,padding:22,border:'1px solid #735f9d',borderRadius:18,background:'#111'},center:{minHeight:'70vh',display:'grid',placeContent:'center',textAlign:'center',padding:24},error:{padding:12,border:'1px solid #a33',borderRadius:10},timer:{display:'flex',gap:14,alignItems:'center',flexWrap:'wrap',padding:14,border:'1px solid #444',borderRadius:12},guide:{display:'grid',gap:10,padding:16,border:'1px solid #735f9d',borderRadius:14},grid:{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:12},check:{display:'block',margin:'8px 0'},stop:{marginTop:18,border:'1px solid #a33',padding:10,background:'transparent'}};
