'use client';
import {useEffect,useMemo,useRef,useState,type CSSProperties} from 'react';
import {ExperienceDirector,QuestRuntime,createQuestCatalog,type ExperienceDirective,type PlayerContext,type SessionSnapshot} from '@hnk/quest-engine';
import {createBundledQuestLibrary,type RuntimeQuestBundle} from '@hnk/quest-library';
import {assertMahasiahProgress,loadDay021PracticeSessionV1,sealDay021V1,startDay021PracticeSessionV1} from '@hnk/supabase-client';
import {createWebQuestSnapshotStore} from '../_runtime/quest-snapshot-store';
import {createWebVaultTextPort} from '../_runtime/WebVaultTextPort';
import {useWebHnkRuntime} from '../_runtime/WebHnkRuntime';

type Session=NonNullable<Awaited<ReturnType<typeof loadDay021PracticeSessionV1>>>;type Seal=Extract<Awaited<ReturnType<typeof sealDay021V1>>,{ok:true}>['response'];type Block='jachin_prana_line'|'boaz_discrimination'|'middle_integration';type Response='STRONG'|'WEAK'|'ABSENT';
interface Draft{jachinDuration:number;naturalNasal:boolean;noRetention:boolean;visualized:boolean;visualizationEase:number;bodyPresence:number;sensation:Response;observationRef:string|null;boazDuration:number;noSpecial:boolean;noIncreasedVolume:boolean;noHolding:boolean;recordedAbsence:boolean;suspended:boolean;notBiomedical:boolean;discriminationRef:string|null;middleDuration:number;middleNatural:boolean;psalm344:boolean;handsFeet:boolean;reoriented:boolean;presence:number;actionDefined:boolean;integrationRef:string|null;soulRef:string|null;safetyStopped:boolean}
const emptyDraft:Draft={jachinDuration:0,naturalNasal:false,noRetention:false,visualized:false,visualizationEase:5,bodyPresence:5,sensation:'ABSENT',observationRef:null,boazDuration:0,noSpecial:false,noIncreasedVolume:false,noHolding:false,recordedAbsence:false,suspended:false,notBiomedical:false,discriminationRef:null,middleDuration:0,middleNatural:false,psalm344:false,handsFeet:false,reoriented:false,presence:5,actionDefined:false,integrationRef:null,soulRef:null,safetyStopped:false};
function canon(bundle:RuntimeQuestBundle,d:ExperienceDirective){return(d.phase.source.block_ids??[]).map(id=>{const b=bundle.canon.blocks.find(x=>x.id===id);if(!b)throw new Error(`canonical_block_missing:${id}`);return b.text})}
function keys(u:string){return{snapshot:`hnk:quest:d021:${u}:snapshot:v1`,session:`hnk:quest:d021:${u}:session:v1`,cache:`hnk:quest:d021:${u}:session-cache:v1`,draft:`hnk:quest:d021:${u}:draft:v1`}}
function parse<T>(k:string):T|null{try{const raw=localStorage.getItem(k);return raw?JSON.parse(raw) as T:null}catch{return null}}function clientId(u:string){return`hnk-web-d021-v1-${u}-${Date.now()}-${Math.random().toString(36).slice(2,8)}`}function clock(elapsed:number,max:number){const left=Math.max(0,max-elapsed);return`${String(Math.floor(left/60)).padStart(2,'0')}:${String(left%60).padStart(2,'0')}`}
export function Day021GoldenV1Web(){const auth=useWebHnkRuntime();const live=Boolean(auth.configured&&auth.phase==='signed-in'&&auth.client&&auth.userId);const runtimeRef=useRef<QuestRuntime|null>(null);const[bundle,setBundle]=useState<RuntimeQuestBundle|null>(null);const[snapshot,setSnapshot]=useState<SessionSnapshot|null>(null);const[session,setSession]=useState<Session|null>(null);const[draft,setDraft]=useState<Draft>(emptyDraft);const[loaded,setLoaded]=useState(false);const[active,setActive]=useState<Block|null>(null);const[started,setStarted]=useState<number|null>(null);const[now,setNow]=useState(Date.now());const[observationNote,setObservationNote]=useState('');const[discriminationNote,setDiscriminationNote]=useState('');const[integrationNote,setIntegrationNote]=useState('');const[soulNote,setSoulNote]=useState('');const[voluntary,setVoluntary]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState<string|null>(null);const[sealed,setSealed]=useState<Seal|null>(null);const vault=useMemo(()=>auth.userId?createWebVaultTextPort(auth.client??null):null,[auth.client,auth.userId]);const context=useMemo<PlayerContext>(()=>({mediationMode:'HNK_CANONICAL',accessibility:{reducedMotion:window.matchMedia?.('(prefers-reduced-motion: reduce)').matches===true,audioEnabled:false,microphoneAvailable:false},offline:!live}),[live]);
useEffect(()=>{if(!auth.userId)return;let ok=true;const lib=createBundledQuestLibrary(),cat=createQuestCatalog(lib),store=createWebQuestSnapshotStore(),k=keys(auth.userId);void Promise.all([cat.requireDay(21),lib.loadBundle(21),store.load(k.snapshot)]).then(([def,b,saved])=>{if(!ok||!b)return;const r=new QuestRuntime(def);runtimeRef.current=r;setBundle(b);setSnapshot(saved?r.restore(saved):r.start());setDraft(parse<Draft>(k.draft)??emptyDraft);const c=parse<Session>(k.cache);if(c)setSession(c);setLoaded(true)}).catch(e=>ok&&setError(e instanceof Error?e.message:'day021_load_failed'));return()=>{ok=false}},[auth.userId]);
useEffect(()=>{if(!auth.userId||!snapshot)return;const k=keys(auth.userId),store=createWebQuestSnapshotStore();if(snapshot.runState==='COMPLETE'){void store.remove(k.snapshot);localStorage.removeItem(k.draft);localStorage.removeItem(k.session);localStorage.removeItem(k.cache)}else void store.save(k.snapshot,snapshot)},[auth.userId,snapshot]);useEffect(()=>{if(auth.userId&&loaded&&snapshot?.runState!=='COMPLETE')localStorage.setItem(keys(auth.userId).draft,JSON.stringify(draft))},[auth.userId,draft,loaded,snapshot?.runState]);useEffect(()=>{const id=window.setInterval(()=>setNow(Date.now()),500);return()=>window.clearInterval(id)},[]);useEffect(()=>{const f=()=>{if(document.visibilityState!=='visible'&&started!==null){setStarted(null);setActive(null);setError('bloco_interrompido_ao_sair_da_aba__tempo_corrente_descartado')}};document.addEventListener('visibilitychange',f);return()=>document.removeEventListener('visibilitychange',f)},[started]);
useEffect(()=>{if(!live||!auth.client||!auth.userId)return;const k=keys(auth.userId),id=localStorage.getItem(k.session);if(!id)return;void loadDay021PracticeSessionV1(auth.client,id).then(s=>{if(s){setSession(s);localStorage.setItem(k.cache,JSON.stringify(s))}}).catch(()=>undefined)},[auth.client,auth.userId,live]);
const director=useMemo(()=>bundle?new ExperienceDirector(bundle.quest):null,[bundle]);const directive=useMemo(()=>director&&snapshot?.currentPhaseId?director.resolvePhase(snapshot.currentPhaseId,context,snapshot):null,[director,context,snapshot]);const max=active==='middle_integration'?300:600;const elapsed=started===null?0:Math.min(max,Math.max(0,Math.floor((now-started)/1000)));
async function ensureSession(){if(session)return session;if(!live||!auth.client||!auth.userId)throw new Error('online_session_required_before_day021_practice');const s=await startDay021PracticeSessionV1(auth.client,{clientSessionId:clientId(auth.userId),appVersion:'0.1.0-web-day021-golden-v1'});setSession(s);const k=keys(auth.userId);localStorage.setItem(k.session,s.id);localStorage.setItem(k.cache,JSON.stringify(s));return s}async function complete(id:string){setError(null);try{if(id==='jachin_reading')await ensureSession();if(runtimeRef.current)setSnapshot(runtimeRef.current.completePhase(id));window.scrollTo({top:0,behavior:'smooth'})}catch(e){setError(e instanceof Error?e.message:'phase_failed')}}function startBlock(b:Block){if(started!==null)return;setActive(b);setStarted(Date.now());setNow(Date.now());setError(null)}function finishBlock(b:Block){if(active!==b||started===null)return;const m=b==='middle_integration'?300:600,e=Math.min(m,Math.max(0,Math.floor((Date.now()-started)/1000)));if(e<1){setError('practice_requires_at_least_1_second');return}setDraft(d=>b==='jachin_prana_line'?{...d,jachinDuration:e}:b==='boaz_discrimination'?{...d,boazDuration:e}:{...d,middleDuration:e});setStarted(null);setActive(null)}function safetyStop(){setStarted(null);setActive(null);setDraft(d=>({...d,safetyStopped:true}));setError('safety_stop__respire_naturalmente__reoriente_se__retome_somente_se_desejar');if(runtimeRef.current)setSnapshot(runtimeRef.current.safetyStop())}function resume(){setError(null);if(runtimeRef.current)setSnapshot(runtimeRef.current.resume())}
async function savePrivate(kind:'observation'|'discrimination'|'integration'|'soul'){if(!vault||!auth.userId)throw new Error('vault_unavailable');const text=kind==='observation'?observationNote:kind==='discrimination'?discriminationNote:kind==='integration'?integrationNote:soulNote;if(!text.trim())throw new Error(`${kind}_private_note_required`);const e=await vault.saveText({userId:auth.userId,day:21,kind:'journal',plaintext:JSON.stringify({schema:`hnk-day021-${kind}-v1`,text:text.trim()})});if(kind==='observation'){setDraft(d=>({...d,observationRef:e.entryId}));setObservationNote('');await complete('jachin_prana_line')}else if(kind==='discrimination'){setDraft(d=>({...d,discriminationRef:e.entryId}));setDiscriminationNote('');await complete('boaz_discrimination')}else if(kind==='integration'){setDraft(d=>({...d,integrationRef:e.entryId}));setIntegrationNote('');await complete('middle_integration')}else{setDraft(d=>({...d,soulRef:e.entryId}));setSoulNote('');await complete('soul_mirror')}}
async function saveJachin(){if(draft.jachinDuration<1||!draft.naturalNasal||!draft.noRetention||!draft.visualized)throw new Error('day021_jachin_requirements');await savePrivate('observation')}async function saveBoaz(){if(draft.boazDuration<1||!draft.noSpecial||!draft.noIncreasedVolume||!draft.noHolding||!draft.recordedAbsence||!draft.suspended||!draft.notBiomedical)throw new Error('day021_boaz_requirements');await savePrivate('discrimination')}async function saveMiddle(){if(draft.middleDuration<1||!draft.middleNatural||!draft.psalm344||!draft.handsFeet||!draft.reoriented||!draft.actionDefined)throw new Error('day021_middle_requirements');await savePrivate('integration')}
async function seal(){if(!runtimeRef.current||!live||!auth.client||!auth.userId||!session||!vault||!draft.observationRef||!draft.discriminationRef||!draft.integrationRef||!draft.soulRef||!voluntary)return;setBusy(true);setError(null);try{await vault.flushPending(auth.userId);const a=await vault.resolveEntryId(auth.userId,draft.observationRef),b=await vault.resolveEntryId(auth.userId,draft.discriminationRef),c=await vault.resolveEntryId(auth.userId,draft.integrationRef),d=await vault.resolveEntryId(auth.userId,draft.soulRef);if(!a||!b||!c||!d)throw new Error('vault_sync_required_before_completion');const result=await sealDay021V1(auth.client,{evidence:{sessionId:session.id,mode:'first_completion',jachinDurationSeconds:draft.jachinDuration,naturalNasalBreathingConfirmed:draft.naturalNasal,noBreathRetentionConfirmed:draft.noRetention,ketherToMalkuthVisualizationConfirmed:draft.visualized,visualizationEaseScore:draft.visualizationEase,bodyPresenceScore:draft.bodyPresence,sensationResponse:draft.sensation,observationVaultEntryRef:a,boazDurationSeconds:draft.boazDuration,noSpecialSensationRequiredConfirmed:draft.noSpecial,noIncreasedBreathVolumeConfirmed:draft.noIncreasedVolume,noBreathHoldingConfirmed:draft.noHolding,sensationOrAbsenceRecordedConfirmed:draft.recordedAbsence,interpretationSuspendedConfirmed:draft.suspended,pranaKiNotBiomedicalFactConfirmed:draft.notBiomedical,discriminationVaultEntryRef:b,middleDurationSeconds:draft.middleDuration,middleNaturalBreathingConfirmed:draft.middleNatural,psalm344PrayerConfirmed:draft.psalm344,handsFeetMovedConfirmed:draft.handsFeet,environmentReorientationConfirmed:draft.reoriented,presenceScore:draft.presence,concreteActionDefinedConfirmed:draft.actionDefined,integrationVaultEntryRef:c,soulMirrorCompleted:true,breathImageSensationInterpretationActionVaultEntryRef:d,safetyStopOccurred:draft.safetyStopped},totalDurationSeconds:draft.jachinDuration+draft.boazDuration+draft.middleDuration});if(!result.ok)throw new Error(result.code);assertMahasiahProgress(result.response.crown,1);setSealed(result.response);setSnapshot(runtimeRef.current.confirmServerCompletion())}catch(e){setError(e instanceof Error?e.message:'day021_completion_failed')}finally{setBusy(false)}}
if(!auth.userId)return <Center text="ENTRE NO ÁTRIO PARA INICIAR O DIA 021."/>;if(!bundle||!snapshot||!director)return <Center text={error??'CARREGANDO DAY 021…'}/>;if(snapshot.runState==='SAFETY_STOP')return <main style={styles.center}><h1>PRÁTICA INTERROMPIDA</h1><p>Respire naturalmente e reoriente-se. Retome apenas se estiver confortável.</p><button onClick={resume}>RETOMAR</button></main>;if(snapshot.runState==='COMPLETE')return <main style={styles.center}><small>DIA 021 · MAHASIAH 1/5</small><h1>LINHA DE PRANA CONCLUÍDA</h1><p>{sealed?.first_completion?`+${sealed.xp_awarded} XP canônicos.`:'Revisita sem novo XP.'}</p><p>Mahasiah 1/5 confirmado pelo servidor · fragmento V ainda apagado · Coroa {sealed?.crown.fragments_lit??4}/7.</p><p>Sensação ausente também é dado válido.</p></main>;if(!directive)return <Center text="FASE INDISPONÍVEL."/>;const p=directive.phase,texts=directive.requiresCanonicalContent?canon(bundle,directive):[],block=p.id as Block,isBlock=['jachin_prana_line','boaz_discrimination','middle_integration'].includes(p.id),done=block==='jachin_prana_line'?draft.jachinDuration>0:block==='boaz_discrimination'?draft.boazDuration>0:draft.middleDuration>0,blockMax=block==='middle_integration'?300:600;
return <main style={styles.shell}><small>DIA 021 · KETHER · MAHASIAH 1/5</small><h1>A LINHA DE PRANA (KI)</h1><p>{live?'VAULT E2EE · ONLINE':'VAULT E2EE · FILA CIFRADA OFFLINE'} · PRANA/KI = MODELO OPERACIONAL HNK</p>{error?<p style={styles.error}>{error}</p>:null}<section style={styles.panel}><small>{p.type} · {p.id}</small>{p.type==='NARRATIVE'?<><h2>Observar sem fabricar.</h2><p>STRONG, WEAK e ABSENT são igualmente válidos. Sensação não é diagnóstico nem prova biomédica ou espiritual automática.</p></>:null}{p.type==='READ'?texts.map((t,i)=><p key={i}>{t}</p>):null}{isBlock?<><div style={styles.timer}>{done?`${block==='jachin_prana_line'?draft.jachinDuration:block==='boaz_discrimination'?draft.boazDuration:draft.middleDuration}s ✓`:clock(active===block?elapsed:0,blockMax)}</div>{active===block?<button onClick={()=>finishBlock(block)}>ENCERRAR ESTE BLOCO</button>:done?<b>BLOCO REGISTRADO ✓</b>:<button onClick={()=>startBlock(block)}>INICIAR BLOCO</button>}<button onClick={safetyStop}>SAFETY STOP</button></>:null}
{p.id==='jachin_prana_line'?<><Toggle label="Respiração nasal natural" v={draft.naturalNasal} on={()=>setDraft(d=>({...d,naturalNasal:!d.naturalNasal}))}/><Toggle label="Sem retenção do ar" v={draft.noRetention} on={()=>setDraft(d=>({...d,noRetention:!d.noRetention}))}/><Toggle label="Visualização Kether → Malkuth realizada" v={draft.visualized} on={()=>setDraft(d=>({...d,visualized:!d.visualized}))}/><Score label="Facilidade da visualização" v={draft.visualizationEase} set={v=>setDraft(d=>({...d,visualizationEase:v}))}/><Score label="Presença corporal" v={draft.bodyPresence} set={v=>setDraft(d=>({...d,bodyPresence:v}))}/><ResponsePicker value={draft.sensation} set={v=>setDraft(d=>({...d,sensation:v}))}/><textarea value={observationNote} onChange={e=>setObservationNote(e.target.value)} placeholder="Nota privada: respiração, imagem e sensação/ausência"/><button onClick={()=>void saveJachin()}>CIFRAR E CONTINUAR</button></>:null}
{p.id==='boaz_discrimination'?<><Toggle label="Nenhuma sensação especial é necessária" v={draft.noSpecial} on={()=>setDraft(d=>({...d,noSpecial:!d.noSpecial}))}/><Toggle label="Não aumentei o volume respiratório" v={draft.noIncreasedVolume} on={()=>setDraft(d=>({...d,noIncreasedVolume:!d.noIncreasedVolume}))}/><Toggle label="Não prendi o ar" v={draft.noHolding} on={()=>setDraft(d=>({...d,noHolding:!d.noHolding}))}/><Toggle label="Registrei sensação ou ausência" v={draft.recordedAbsence} on={()=>setDraft(d=>({...d,recordedAbsence:!d.recordedAbsence}))}/><Toggle label="Suspendi interpretação automática" v={draft.suspended} on={()=>setDraft(d=>({...d,suspended:!d.suspended}))}/><Toggle label="Prana/Ki não são fato biomédico estabelecido" v={draft.notBiomedical} on={()=>setDraft(d=>({...d,notBiomedical:!d.notBiomedical}))}/><textarea value={discriminationNote} onChange={e=>setDiscriminationNote(e.target.value)} placeholder="Nota privada de discriminação"/><button onClick={()=>void saveBoaz()}>CIFRAR E CONTINUAR</button></>:null}
{p.id==='middle_integration'?<><Toggle label="Respiração permaneceu natural" v={draft.middleNatural} on={()=>setDraft(d=>({...d,middleNatural:!d.middleNatural}))}/><Toggle label="Oração com Salmo 34:4 concluída" v={draft.psalm344} on={()=>setDraft(d=>({...d,psalm344:!d.psalm344}))}/><Toggle label="Movi mãos e pés" v={draft.handsFeet} on={()=>setDraft(d=>({...d,handsFeet:!d.handsFeet}))}/><Toggle label="Reorientei-me ao ambiente" v={draft.reoriented} on={()=>setDraft(d=>({...d,reoriented:!d.reoriented}))}/><Score label="Presença corporal atual" v={draft.presence} set={v=>setDraft(d=>({...d,presence:v}))}/><Toggle label="Defini uma ação concreta para hoje" v={draft.actionDefined} on={()=>setDraft(d=>({...d,actionDefined:!d.actionDefined}))}/><textarea value={integrationNote} onChange={e=>setIntegrationNote(e.target.value)} placeholder="Síntese privada + ação concreta"/><button onClick={()=>void saveMiddle()}>CIFRAR E CONTINUAR</button></>:null}
{p.id==='soul_mirror'?<><textarea value={soulNote} onChange={e=>setSoulNote(e.target.value)} placeholder="Respiração · imagem · sensação · interpretação · ação"/><button onClick={()=>void savePrivate('soul')}>CIFRAR ESPELHO</button></>:null}{p.id==='completion'?<><Toggle label="Concluo voluntariamente" v={voluntary} on={()=>setVoluntary(v=>!v)}/><button disabled={busy||!voluntary} onClick={()=>void seal()}>{busy?'SELANDO…':'SELAR DAY 021'}</button></>:null}{!['jachin_prana_line','boaz_discrimination','middle_integration','soul_mirror','completion'].includes(p.id)?<button onClick={()=>void complete(p.id)}>CONTINUAR</button>:null}</section></main>}
function Toggle({label,v,on}:{label:string;v:boolean;on:()=>void}){return <label style={styles.toggle}><input type="checkbox" checked={v} onChange={on}/>{label}</label>}function Score({label,v,set}:{label:string;v:number;set:(v:number)=>void}){return <label>{label}: <b>{v}/10</b><input type="range" min={0} max={10} step={1} value={v} onChange={e=>set(Number(e.target.value))}/></label>}function ResponsePicker({value,set}:{value:Response;set:(v:Response)=>void}){return <div><p>Resposta percebida</p>{(['STRONG','WEAK','ABSENT'] as const).map(x=><button key={x} onClick={()=>set(x)} aria-pressed={value===x}>{x}</button>)}</div>}function Center({text}:{text:string}){return <main style={styles.center}>{text}</main>}const styles:Record<string,CSSProperties>={shell:{maxWidth:900,margin:'0 auto',padding:24,fontFamily:'system-ui',lineHeight:1.6},center:{minHeight:'70vh',display:'grid',placeContent:'center',gap:12,padding:24,textAlign:'center'},panel:{display:'grid',gap:14,padding:24,border:'1px solid #666',borderRadius:18},timer:{fontSize:48,fontWeight:800},toggle:{display:'flex',gap:10,alignItems:'center'},error:{fontWeight:700}}
