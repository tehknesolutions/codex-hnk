import Link from 'next/link';
import {conceptRegistry} from '../convergence';
import {labDomains} from '../knowledge';
export default function ConceptsPage(){return <main className="constellation-stage"><header className="constellation-head"><Link href="/laboratorio">← Grimório</Link><div><p>HNK · CONCEPT REGISTRY</p><h1>Índice de Conceitos</h1></div><span>{conceptRegistry.length} conceitos indexados</span></header><section className="constellation-book"><article className="constellation-page"><p className="folio-kicker">CORPUS → CONCEITO → DOMÍNIO</p><h2>Registro navegável</h2><p className="constellation-rule">Este índice reúne somente conceitos já presentes nos registros documentais carregados pelo Laboratório. Presença no índice não equivale a aprovação canônica.</p></article><article className="constellation-page relation-page"><div className="relation-scroll">{conceptRegistry.map(c=><div className="relation-card" key={c.id}><header><Link href={'/laboratorio/conceitos/'+c.id}><span>{c.id}</span></Link><b>{c.origin}</b></header><p>{c.term??'Termo não registrado'}</p><small>{c.domains.map(id=>labDomains.find(d=>d.id===id)?.name??id).join(' · ')||'Domínio não resolvido'}</small></div>)}</div></article></section></main>}
