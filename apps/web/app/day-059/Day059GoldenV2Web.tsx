'use client';
import {useEffect,useMemo,useState,type ReactNode} from 'react';
import {loadDay059PracticeSessionV2,sealDay059V2,startDay059PracticeSessionV2} from '@hnk/supabase-client';
import type {Day059SafetyReason} from '@hnk/practice-contract';
import {createWebVaultTextPort} from '../_runtime/WebVaultTextPort';
import {useWebHnkRuntime} from '../_runtime/WebHnkRuntime';

type Session=NonNullable<Awaited<ReturnType<typeof loadDay059PracticeSessionV2>>>;type Phase='BLUE'|'GRAY';
type Blue={imagePresent:boolean;returnCount:number;sharpness:number;brightness:number;ocularEffort:number;silence:number;focus:boolean;noForce:boolean;noEyePressure:boolean;noForcedEffect:boolean};
type Gray={imagePresent:boolean;returnCount:number;sharpness:number;brightness:number;ocularEffort:number;expectation:number;neutral:boolean;samePosture:boolean;sameEnvironment:boolean;notSabotaged:boolean};
type Draft={blueDone:boolean;grayDone:boolean;blue:Blue;gray:Gray;comparison:boolean;afterBoth:boolean;layers:boolean;noClairvoyance:boolean;reason:boolean;noExternalProof:boolean;absenceValid:boolean;noHighImpact:boolean;psalm:boolean;formula:boolean;instrumental:boolean;sensation:boolean;symbol:boolean;hypothesis:boolean;thanks:boolean;returnConfirmed:boolean;grounding:boolean;privateRef:string|null;voluntary:boolean;finalSafety:boolean;safetyStop:boolean;safetyReason:Day059SafetyReason};
const blue0:Blue={imagePresent:false,returnCount:0,sharpness:0,brightness:0,ocularEffort:0,silence:0,focus:false,noForce:false,noEyePressure:false,noForcedEffect:false};const gray0:Gray={imagePresent:false,returnCount:0,sharpness:0,brightness:0,ocularEffort:0,expectation:0,neutral:false,samePosture:false,sameEnvironment:false,notSabotaged:false};
const empty:Draft={blueDone:false,grayDone:false,blue:blue0,gray:gray0,comparison:false,afterBoth:false,layers:false,noClairvoyance:false,reason:false,noExternalProof:false,absenceValid:false,noHighImpact:false,psalm:false,formula:false,instrumental:false,sensation:false,symbol:false,hypothesis:false,thanks:false,returnConfirmed:false,grounding:false,privateRef:null,voluntary:false,finalSafety:false,safetyStop:false,safetyReason:'NONE'};
const LIMIT=900,card={background:'#121217',border:'1px solid #34313f',borderRadius:18,padding:18,margin:'14px 0'} as const,button={background:'#d9b86c',color:'#17120a',border:0,borderRadius:10,padding:'10px 13px',fontWeight:800,cursor:'pointer'} as const;
function dk(u:string){return`hnk:day059:${u}:draft:v2`}function sk(u:string){return`hnk:day059:${u}:session:v2`}function parse<T>(v:string|null):T|null{try{return v?JSON.parse(v) as T:null}catch{return null}}function cid(u:string){return`hnk-web-d059-v2-${u}-${Date.now()}-${Math.random().toString(36).slice(2,8)}`}function clock(s:number){return`${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`}function clamp(v:number){return Number.isFinite(v)?Math.max(0,Math.min(10,Math.trunc(v))):0}
export function Day059GoldenV2Web(){
 const auth=useWebHnkRuntime(),live=Boolean(auth.configured&&auth.phase==='signed-in'&&auth.client&&auth.userId),vault=useMemo(()=>auth.userId?createWebVaultTextPort(auth.client??null):null,[auth.client,auth.userId]);
 const[session,setSession]=useState<Session|null>(null),[d,setD]=useState<Draft>(empty),[loaded,setLoaded]=useState(false),[phase,setPhase]=useState<Phase|null>(null),[started,setStarted]=useState<number|null>(null),[now,setNow]=useState(Date.now()),[privateText,setPrivateText]=useState(''),[error,setError]=useState<string|null>(null),[busy,setBusy]=useState(false),[complete,setComplete]=useState(false);
 useEffect(()=>{if(!auth.userId)return;setD(parse<Draft>(localStorage.getItem(dk(auth.userId)))??empty);setLoaded(true);const id=localStorage.getItem(sk(auth.userId));if(id&&auth.client)void loadDay059PracticeSessionV2(auth.client,id).then(s=>s?setSession(s):localStorage.removeItem(sk(auth.userId!))).catch(()=>undefined)},[auth.client,auth.userId]);
 useEffect(()=>{if(auth.userId&&loaded&&!complete)localStorage.setItem(dk(auth.userId),JSON.stringify(d))},[auth.userId,d,loaded,complete]);useEffect(()=>{const id=window.setInterval(()=>setNow(Date.now()),250);return()=>window.clearInterval(id)},[]);
 useEffect(()=>{if(!phase||started===null)return;if(Math.floor((now-started)/1000)>=LIMIT)finish(phase)},[phase,started,now]);
 useEffect(()=>{const h=()=>{if(document.visibilityState==='visible')return;if(phase){setPhase(null);setStarted(null);setError('segmento_interrompido__tempo_corrente_descartado')}setPrivateText('')};document.addEventListener('visibilitychange',h);return()=>document.removeEventListener('visibilitychange',h)},[phase]);
 function patch(p:Partial<Draft>){setD(x=>({...x,...p}))}function pb(p:Partial<Blue>){setD(x=>({...x,blue:{...x.blue,...p}}))}function pg(p:Partial<Gray>){setD(x=>({...x,gray:{...x.gray,...p}}))}
 const elapsed=started===null?0:Math.min(LIMIT,Math.max(0,Math.floor((now-started)/1000)));
 function begin(p:Phase){if(phase||(p==='BLUE'&&d.blueDone)||(p==='GRAY'&&(!d.blueDone||d.grayDone))){setError('day059_timer_sequence_violation');return}setError(null);setPhase(p);setStarted(Date.now());setNow(Date.now())}
 function finish(p:Phase){patch(p==='BLUE'?{blueDone:true}:{grayDone:true});setPhase(null);setStarted(null)}
 function stop(){if(!phase){setError('day059_no_active_segment');return}if(d.safetyReason==='NONE'){setError('day059_safety_reason_required');return}setPhase(null);setStarted(null);setPrivateText('');patch({safetyStop:true,finalSafety:false});setError('safety_stop_registered__segment_not_counted')}
 async function ensureSession(){if(session)return session;if(!live||!auth.client||!auth.userId)throw new Error('online_session_required_before_day059_practice');const s=await startDay059PracticeSessionV2(auth.client,{clientSessionId:cid(auth.userId),appVersion:'0.1.0-web-day059-v2'});setSession(s);localStorage.setItem(sk(auth.userId),s.id);return s}
 async function savePrivate(){if(!privateText.trim())throw new Error('private_note_required');if(!vault||!auth.userId)throw new Error('vault_unavailable');await ensureSession();const e=await vault.saveText({userId:auth.userId,day:59,kind:'journal',plaintext:JSON.stringify({schema:'hnk-day059-private-association-v2',text:privateText.trim()})});patch({privateRef:e.entryId});setPrivateText('')}
 async function resolveOptional(){if(!d.privateRef)return undefined;if(!vault||!auth.userId)throw new Error('vault_unavailable');await vault.flushPending(auth.userId);const x=await vault.resolveEntryId(auth.userId,d.privateRef);if(!x)throw new Error('private_vault_sync_required');return x}
 async function seal(){if(!auth.client||!auth.userId)return;setBusy(true);setError(null);try{const s=await ensureSession(),privateRef=await resolveOptional();const r=await sealDay059V2(auth.client,{evidence:{sessionId:s.id,blue:{measuredSeconds:900,imagePresent:d.blue.imagePresent,returnCount:d.blue.returnCount,sharpness:d.blue.sharpness,brightness:d.blue.brightness,ocularEffort:d.blue.ocularEffort,silence:d.blue.silence,bluePearlFocusConfirmed:d.blue.focus,thoughtsReleasedWithoutForceConfirmed:d.blue.noForce,noEyePressureConfirmed:d.blue.noEyePressure,noForcedBrightnessOrSilenceConfirmed:d.blue.noForcedEffect},gray:{measuredSeconds:900,imagePresent:d.gray.imagePresent,returnCount:d.gray.returnCount,sharpness:d.gray.sharpness,brightness:d.gray.brightness,ocularEffort:d.gray.ocularEffort,expectation:d.gray.expectation,neutralGrayPointConfirmed:d.gray.neutral,samePostureConfirmed:d.gray.samePosture,sameEnvironmentConfirmed:d.gray.sameEnvironment,controlNotSabotagedConfirmed:d.gray.notSabotaged},...(privateRef?{privateVaultEntryRef:privateRef}:{}),comparisonCompletedConfirmed:d.comparison,comparedOnlyAfterBothConfirmed:d.afterBoth,interpretationSeparatedConfirmed:d.layers,clairvoyanceNotClaimedConfirmed:d.noClairvoyance,reasonPreservedConfirmed:d.reason,differenceNotExternalMechanismProofConfirmed:d.noExternalProof,absenceValidConfirmed:d.absenceValid,highImpactNotUsedConfirmed:d.noHighImpact,psalm101Confirmed:d.psalm,heHeAlephConfirmed:d.formula,instrumentalObservationNamedConfirmed:d.instrumental,sensationNamedConfirmed:d.sensation,traditionalSymbolNamedConfirmed:d.symbol,openHypothesisNamedConfirmed:d.hypothesis,thanksToGodConfirmed:d.thanks,consciousReturnConfirmed:d.returnConfirmed,groundingThreeObjectsConfirmed:d.grounding,voluntaryCompletionConfirmed:d.voluntary,finalSafetyClearConfirmed:d.finalSafety,safetyStopOccurred:d.safetyStop,safetyStopReason:d.safetyReason}});if(!r.ok)throw new Error(r.code);setComplete(true);localStorage.removeItem(dk(auth.userId));localStorage.removeItem(sk(auth.userId))}catch(e){setError(e instanceof Error?e.message:'day059_completion_failed')}finally{setBusy(false)}}
 if(!auth.userId)return <main style={{padding:32,color:'#eee'}}>ENTRE NO ÁTRIO PARA INICIAR O DAY 059.</main>;if(complete)return <main style={{padding:32,color:'#eee'}}><h1>Day 059 concluído</h1><p>Hahaiah III registrado. Próximo: Day 060.</p></main>;
 const boundaries=[d.comparison,d.afterBoth,d.layers,d.noClairvoyance,d.reason,d.noExternalProof,d.absenceValid,d.noHighImpact,d.psalm,d.formula,d.instrumental,d.sensation,d.symbol,d.hypothesis,d.thanks,d.returnConfirmed,d.grounding,d.voluntary,d.finalSafety].every(Boolean);const ready=d.blueDone&&d.grayDone&&d.blue.focus&&d.blue.noForce&&d.blue.noEyePressure&&d.blue.noForcedEffect&&d.gray.neutral&&d.gray.samePosture&&d.gray.sameEnvironment&&d.gray.notSabotaged&&boundaries;
 return <main style={{maxWidth:900,margin:'0 auto',padding:'28px 18px 72px',fontFamily:'Inter,system-ui,sans-serif',color:'#eee'}}><p style={{letterSpacing:2,opacity:.7}}>CHOKMAH · HAHAIAH III · DAY 059</p><h1>Meditação da Pérola Azul — 15 min + controle cinza</h1><p>Imagem ausente é resultado válido. Brilho, cor, fosfeno ou intensidade subjetiva não provam clarividência nem objeto externo.</p>{error&&<p role="alert" style={{color:'#ff9da8'}}>{error}</p>}
 <Card title="Pérola Azul · 15:00"><Timer active={phase==='BLUE'} done={d.blueDone} elapsed={phase==='BLUE'?elapsed:0} start={()=>begin('BLUE')}/><Check l="Foco na Pérola Azul" v={d.blue.focus} set={v=>pb({focus:v})}/><Check l="Pensamentos reconhecidos sem força" v={d.blue.noForce} set={v=>pb({noForce:v})}/><Check l="Sem pressão nos olhos/testa" v={d.blue.noEyePressure} set={v=>pb({noEyePressure:v})}/><Check l="Sem forçar brilho ou silêncio" v={d.blue.noForcedEffect} set={v=>pb({noForcedEffect:v})}/><Check l="Imagem presente (opcional; ausência é válida)" v={d.blue.imagePresent} set={v=>pb({imagePresent:v})}/><Metrics labels={['returnCount','sharpness','brightness','ocularEffort','silence']} data={d.blue} set={pb}/></Card>
 <Card title="Controle cinza · 15:00"><Timer active={phase==='GRAY'} done={d.grayDone} elapsed={phase==='GRAY'?elapsed:0} start={()=>begin('GRAY')}/><Check l="Ponto cinza neutro" v={d.gray.neutral} set={v=>pg({neutral:v})}/><Check l="Mesma postura" v={d.gray.samePosture} set={v=>pg({samePosture:v})}/><Check l="Mesmo ambiente" v={d.gray.sameEnvironment} set={v=>pg({sameEnvironment:v})}/><Check l="Controle não sabotado" v={d.gray.notSabotaged} set={v=>pg({notSabotaged:v})}/><Check l="Imagem presente (opcional; ausência é válida)" v={d.gray.imagePresent} set={v=>pg({imagePresent:v})}/><Metrics labels={['returnCount','sharpness','brightness','ocularEffort','expectation']} data={d.gray} set={pg}/></Card>
 <Card title="Comparação"><Check l="Comparação concluída" v={d.comparison} set={v=>patch({comparison:v})}/><Check l="Comparação somente após as duas condições" v={d.afterBoth} set={v=>patch({afterBoth:v})}/><Check l="Observação e interpretação separadas" v={d.layers} set={v=>patch({layers:v})}/><Check l="Clarividência não alegada" v={d.noClairvoyance} set={v=>patch({noClairvoyance:v})}/><Check l="Razão preservada" v={d.reason} set={v=>patch({reason:v})}/><Check l="Diferença não tratada como prova externa" v={d.noExternalProof} set={v=>patch({noExternalProof:v})}/><Check l="Ausência aceita como dado válido" v={d.absenceValid} set={v=>patch({absenceValid:v})}/><Check l="Sem uso de alto impacto" v={d.noHighImpact} set={v=>patch({noHighImpact:v})}/></Card>
 <Card title="Coluna do Meio e retorno"><Check l="Salmo 10:1" v={d.psalm} set={v=>patch({psalm:v})}/><Check l="HE–HE–ALEPH" v={d.formula} set={v=>patch({formula:v})}/><Check l="Observação instrumental nomeada" v={d.instrumental} set={v=>patch({instrumental:v})}/><Check l="Sensação nomeada" v={d.sensation} set={v=>patch({sensation:v})}/><Check l="Símbolo tradicional nomeado" v={d.symbol} set={v=>patch({symbol:v})}/><Check l="Hipótese permanece aberta" v={d.hypothesis} set={v=>patch({hypothesis:v})}/><Check l="Agradecimento a Deus" v={d.thanks} set={v=>patch({thanks:v})}/><Check l="Retorno consciente" v={d.returnConfirmed} set={v=>patch({returnConfirmed:v})}/><Check l="Grounding: três objetos reais" v={d.grounding} set={v=>patch({grounding:v})}/></Card>
 <Card title="Nota privada opcional"><textarea value={privateText} onChange={e=>setPrivateText(e.target.value)} placeholder="Associação privada opcional — memória volátil até Vault E2EE" style={{width:'100%',minHeight:90}}/><button style={button} disabled={Boolean(d.privateRef)} onClick={()=>void savePrivate().catch(e=>setError(e instanceof Error?e.message:String(e)))}>Selar no Vault e limpar</button>{d.privateRef&&<p style={{opacity:.7}}>Nota privada selada.</p>}</Card>
 <Card title="Fechamento e segurança"><Check l="Conclusão voluntária" v={d.voluntary} set={v=>patch({voluntary:v})}/><Check l="Estado final seguro e claro" v={d.finalSafety} set={v=>patch({finalSafety:v})}/><label>Safety stop <select value={d.safetyReason} onChange={e=>patch({safetyReason:e.target.value as Day059SafetyReason})}><option value="NONE">NONE</option><option value="OCULAR_DISCOMFORT">OCULAR_DISCOMFORT</option><option value="HEADACHE">HEADACHE</option><option value="VERTIGO">VERTIGO</option><option value="ANXIETY_INCREASED">ANXIETY_INCREASED</option><option value="DISORIENTATION">DISORIENTATION</option><option value="OTHER">OTHER</option></select></label><button style={{...button,marginLeft:10,background:'#ff9da8'}} onClick={stop}>Safety stop</button></Card>
 <button style={{...button,marginTop:18,fontSize:16}} disabled={!ready||busy||Boolean(phase)} onClick={()=>void seal()}>{busy?'Selando…':'Concluir Day 059'}</button>{!ready&&<p style={{opacity:.7}}>Exige 15 min azul + 15 min cinza, comparação sóbria, retorno e segurança.</p>}</main>
}
function Card({title,children}:{title:string;children:ReactNode}){return <section style={card}><h2 style={{marginTop:0}}>{title}</h2>{children}</section>}
function Check({l,v,set}:{l:string;v:boolean;set:(v:boolean)=>void}){return <label style={{display:'block',margin:'8px 0'}}><input type="checkbox" checked={v} onChange={e=>set(e.target.checked)}/> {l}</label>}
function Timer({active,done,elapsed,start}:{active:boolean;done:boolean;elapsed:number;start:()=>void}){return <p><strong>{done?'15:00':clock(elapsed)}</strong> <button style={button} disabled={active||done} onClick={start}>{done?'Concluído':active?'Em foco…':'Iniciar 15 min'}</button></p>}
function Metrics<T extends Record<string,unknown>>({labels,data,set}:{labels:string[];data:T;set:(p:Partial<T>)=>void}){return <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(135px,1fr))',gap:8}}>{labels.map(k=><label key={k}>{k}<input type="number" min={0} max={k==='returnCount'?999:10} value={Number(data[k])} onChange={e=>set({[k]:k==='returnCount'?Math.max(0,Math.trunc(Number(e.target.value)||0)):clamp(Number(e.target.value))} as Partial<T>)}/></label>)}</div>}
